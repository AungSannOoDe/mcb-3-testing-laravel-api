document.addEventListener('DOMContentLoaded', function () {
    // Select the button
    const btn = document.querySelector('.modalBtn');

    // Trigger click
    if (btn) {
        btn.click();
    }
});